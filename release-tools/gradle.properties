# JReleaser 1.25 validates that every enabled release provider has a non-empty
# token even when jreleaserDeploy is used with skipTag/skipRelease. This value
# is deliberately not a credential and has no permissions.
jreleaser.github.token=not-used-maven-central-only
