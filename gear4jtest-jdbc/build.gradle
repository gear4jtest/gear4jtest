dependencies {
    api project(':gear4jtest-core')

    api libs.jackson
    implementation libs.slf4j

    testImplementation libs.jackson.datatype.jsr310
}

ext.moduleName = 'io.github.gear4jtest.jdbc'

tasks.named('integrationTest') {
    systemProperty 'gear4j.test.sqlPlanReportDirectory',
        layout.buildDirectory.dir('reports/sql-plan-qualification').get().asFile.absolutePath
}
